# Dependencies
node_modules
.pnpm-store

# Build outputs
dist
.next
out

# Environment — NEVER COMMIT THESE
.env
.env.*
!.env.example
!.env.production.example

# Terraform — state and vars contain secrets
infra/terraform/.terraform/
infra/terraform/terraform.tfstate
infra/terraform/terraform.tfstate.backup
infra/terraform/.terraform.lock.hcl
infra/terraform/terraform.tfvars

# JWT keys — NEVER COMMIT PRIVATE KEY
apps/api/keys/private.pem
apps/api/keys/public.pem

# SSL certs
infra/certs/
*.pem
!apps/api/keys/public.pem  # public key is safe to commit

# Editor
.vscode
.idea
*.swp

# OS
.DS_Store
Thumbs.db

# Test
coverage
*.tsbuildinfo

# Docker
*.log

# pnpm
.npmrc

# extra 
extra.md
